import React from 'react';
import cl from './css/News.module.css';

const News = (props) => {
    return(
        <section className = {cl .news}>
           our news
        </section>
    );
}

export default News;