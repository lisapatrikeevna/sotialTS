import React from 'react';
import cl from './css/Music.module.css';

const Music = (props) =>{
    return(
        <section className = {cl .music}>
            all music
        </section>
    );
}

export default Music;